# Roadmap

方向按“能明显降低用户决策或安装成本”排序；不为功能数量扩张界面。

## 下一步

1. **兼容性矩阵**：在隔离 profile 中按 DSH 版本和操作系统真实安装、启动，展示最近一次结果和日志证据。
2. **更新检测**：比较已安装精确来源与最新通过检查的版本，提供同样带预检和回滚的一键升级。
3. **更强入库证明**：检查 patch 与宿主产物是否存在，并为构建产物生成来源证明、SBOM 和哈希。
4. **推荐治理**：增加公开提名模板、复核周期、利益披露和过期自动降级，避免只按星标排名。
5. **插件健康度**：识别归档、删除、许可证变化和长期不维护，保留历史但停止新安装。
6. **按能力热更新**：只有 DSH 或插件提供明确卸载/重载契约时才开放无重启切换；其他情况继续要求重启 Web。
7. **更完整的发现**：在 GitHub Search 1,000 项上限之外，引入维护者提交和可验证的增量索引。

## 不做

- 不把 GitHub Topic、项目名称或 README 自称当作 DSH 插件证明。
- 不把清单检查、星标或推荐描述成安全审计。
- 不为了展示“专业”加入无助于安装决策的仪表盘和配置项。
- 不执行第三方依赖安装脚本来换取更高的表面安装成功率。
